import { CodeRunner } from "../../components/md-lib/CodeRunner";
import { Bpmn } from "../../components/md-lib/Bpmn";

# Test Page

## Code Runner

<CodeRunner
  code={`
    const str = 'hello world';
    context.log('initial string=', str);
    const reversed = str.split('').reverse('').join('');
    context.log('reversed string=', reversed);
`}
/>


## Bpmn

<Bpmn />
